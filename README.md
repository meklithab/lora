# rankalloc

At a fixed total LoRA adapter parameter count, does non-uniform layer-wise rank allocation beat
uniform allocation? See `BUILD_SPEC.md` for the full experiment design and `CLAUDE.md` for the
working rules.

Quickstart, grid instructions, resume instructions, and figure regeneration commands land here in
P6, once the runner and analysis scripts exist.
